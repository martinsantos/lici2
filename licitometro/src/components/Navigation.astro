---
const pathname = Astro.url.pathname;
---

<nav class="bg-white shadow mb-8">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex">
        <div class="flex-shrink-0 flex items-center">
          <a href="/" class="text-xl font-bold text-gray-900">
            Licitómetro
          </a>
        </div>
        <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
          <a
            href="/licitaciones"
            class={`border-transparent ${pathname.startsWith('/licitaciones') ? 'border-blue-500 text-gray-900' : 'text-gray-500'} hover:border-gray-300 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium`}
          >
            Licitaciones
          </a>
          <a
            href="/recon"
            class={`border-transparent ${pathname.startsWith('/recon') ? 'border-blue-500 text-gray-900' : 'text-gray-500'} hover:border-gray-300 hover:text-gray-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium`}
          >
            RECON
          </a>
        </div>
      </div>
    </div>
  </div>
</nav>
